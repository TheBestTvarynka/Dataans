[package]
name = "web-server"
version = "0.1.0"
description = "Web-server for data synchronization and backup"
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
web-api-types = { workspace = true, features = ["server"] }
serde = { workspace = true, features = ["derive"] }
sqlx = { workspace = true, features = ["runtime-tokio", "time", "uuid", "postgres"] }
thiserror.workspace = true
uuid = { workspace = true, features = ["serde", "v4"] }
tracing.workspace = true
time = { workspace = true }
rocket = { workspace = true, features = ["json"] }

# logging
tracing-subscriber = { version = "0.3", features = ["std", "fmt", "ansi", "env-filter"] }

# utils
hex = "0.4"

# crypto
sha2 = "0.10"
argon2 = { version = "0.5", features = ["std"] }
aes-gcm = { version = "0.10", features = ["std"] }
hmac = "0.12"