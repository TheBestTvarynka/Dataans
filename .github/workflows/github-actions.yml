name: Build & Deploy
on:
  push:
    branches:
    - 'main'
jobs:
  fmt:
    name: Formatting
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: 'Add nightly channel'
      run: 'rustup toolchain install nightly'

    - name: 'Add nightly fmt'
      run: 'rustup component add rustfmt --toolchain nightly-x86_64-unknown-linux-gnu'

    - name: 'Fmt'
      run: 'cargo +nightly fmt --all -- --check'

  clippy:
    name: Clippy
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Set up dependencies
      run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev javascriptcoregtk-4.1 libsoup-3.0 webkit2gtk-4.1

    - name: 'Clippy'
      run: 'SQLX_OFFLINE=true mkdir -p dataans/dist && cargo clippy --workspace --all-targets -- -D warnings'

  test:
    name: Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Set up dependencies
      run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev javascriptcoregtk-4.1 libsoup-3.0 webkit2gtk-4.1

    - name: 'Tests'
      run: 'SQLX_OFFLINE=true cargo test --workspace'

# Some day I'll set up cross platform (matrix) app build with proper artifacts.
# But currently I don't need it :)
  # deploy:
  #   name: Build app
  #   runs-on: ubuntu-latest
  #   needs: check

  #   permissions:
  #     contents: 'read'
  #     id-token: 'write'

  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4
